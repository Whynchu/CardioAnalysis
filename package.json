{
  "name": "cardio-analysis",
  "version": "1.0.0",
  "private": true,
  "description": "Capacitor wrapper for the CardioAnalysis web app (Android).",
  "scripts": {
    "copy:www": "node scripts/copy-www.js",
    "cap:sync": "npm run copy:www && npx cap sync android",
    "cap:add:android": "npm run copy:www && npx cap add android",
    "cap:open:android": "npx cap open android",
    "android:debug": "npm run cap:sync && cd android && .\\gradlew.bat assembleDebug"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  },
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/app": "^6.0.1",
    "@capacitor/core": "^6.1.2",
    "@capacitor/geolocation": "^6.0.1"
  }
}
